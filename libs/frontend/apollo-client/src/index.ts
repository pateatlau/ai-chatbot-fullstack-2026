// Export Apollo Client
export { apolloClient, cache } from './client';

// Export Apollo Client utilities
export { ApolloClient, ApolloProvider, InMemoryCache } from '@apollo/client';
export { gql } from '@apollo/client';
export type { ApolloClientOptions } from '@apollo/client';

// Export all queries and mutations
export * from './queries';

// Export all hooks
export * from './hooks';
